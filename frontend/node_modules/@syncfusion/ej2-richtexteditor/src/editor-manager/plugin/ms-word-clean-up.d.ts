import { EditorManager } from '../base/editor-manager';
import { NotifyArgs } from '../../rich-text-editor/base/interface';
/**
 * PasteCleanup for MsWord content
 * @hidden
 */
export declare class MsWordPaste {
    parent: EditorManager;
    constructor(parent?: EditorManager);
    olData: string[];
    ulData: string[];
    ignorableNodes: string[];
    blockNode: string[];
    listContents: string[];
    addEventListener(): void;
    wordCleanup(e: NotifyArgs): void;
    cleanUp(node: HTMLElement, listNodes: Element[]): Element[];
    listConverter(listNodes: Element[]): void;
    makeConversion(collection: {
        listType: string;
        content: string[];
        nestedLevel: number;
    }[]): HTMLElement;
    getListStyle(listType: string, nestedLevel: number): string;
    getListContent(elem: Element): void;
}
